# SatyaVoice — RunPod deployment environment (TEMPLATE).
#
# Set these in the RunPod **pod template** (Edit Template → Environment
# Variables) or in a .env file if you run the container with docker run.
#
# NEVER put real keys in git — RunPod pod templates are private, but repo
# files are not.

# ---- Environment mode -------------------------------------------------------
VOICETRUST_ENV=production

# ---- CORS: your Vercel frontend origin (exact, no trailing slash) ----------
VOICETRUST_CORS_ORIGINS=https://sih-voice.vercel.app

# ---- PostgreSQL (Neon free tier; keep ?sslmode=require) --------------------
VOICETRUST_DATABASE_URL=postgresql://user:password@ep-xxx.aws.neon.tech/neondb?sslmode=require

# ---- Auth ("tenant:key", comma-separated) ----------------------------------
VOICETRUST_AUTH_API_KEYS=sih-demo:sk_REPLACE_WITH_YOUR_GENERATED_KEY

# ---- Session store -----------------------------------------------------------
# No Redis on a single pod: in-process memory is correct for one worker.
VOICETRUST_SESSION_STORE_BACKEND=memory

# ---- AI modes (mock/manual first; real later — pods with 16GB+ RAM handle it)
VOICETRUST_DETECTOR_MODE=mock
VOICETRUST_ASR_MODE=manual
# Active anti-spoof checkpoint: NII Yamagishi Lab's MMS-300M-AntiDeepfake.
# On a 512MB-class pod, prefer VOICETRUST_DETECTOR_MODE=remote_hf (+
# VOICETRUST_HF_SPACE_ID) so the GPU work happens on the ZeroGPU Space.
VOICETRUST_MODEL_ID=nii-yamagishilab/mms-300m-anti-deepfake
VOICETRUST_ASR_MODEL_SIZE=small
VOICETRUST_ASR_DEVICE=cpu
VOICETRUST_ASR_COMPUTE_TYPE=int8

# ---- Speaker vault / logging ------------------------------------------------
VOICETRUST_SPEAKER_VAULT_ENABLED=true
VOICETRUST_RATE_LIMIT_ENABLED=true
VOICETRUST_LOG_LEVEL=INFO
