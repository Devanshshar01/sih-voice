# --- Phase 1: core API + mock detector (safe to install right away) ---
alembic>=1.13,<2                # Database migrations (alembic/env.py)
PyJWT>=2.8,<3                 # WebSocket JWT authentication (ws_auth.py)
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.31
pydantic>=2.9,<3
sqlalchemy>=2.0,<2.1
python-multipart>=0.0.9
numpy>=2,<3
httpx>=0.27          # used by TestClient and scripts/demo_client.py
websockets>=12.0      # used by scripts/demo_client.py (real WS client)

# --- Optional codec normalization extensions (app/services/codec_normalizer.py) ---
# Core families (PCM float32/s16, G.711 mu-law/A-law) need nothing extra.
# Opus support additionally requires the native libopus shared library:
# opuslib>=2.0.0
# AMR-NB/AMR-WB require a native AMR decoder binding and are correctly
# reported as unsupported until one is installed.

# --- Real detector runtime ---
# The active acoustic deepfake detector (nii-yamagishilab/mms-300m-anti-deepfake)
# is a fairseq Wav2Vec2 front-end + FC head packaged with
# PyTorchModelHubMixin. Versions follow the official model card; fairseq
# 0.12.2 is pinned because it is the version the checkpoint was released
# with. Known caveat: fairseq 0.12.2 predates NumPy 2; if the fairseq import
# fails under NumPy 2, pin numpy<2 (this does not affect mock mode).
# fairseq is only needed when VOICETRUST_DETECTOR_MODE=real.
torch>=2.2
transformers>=4.39,<5
fairseq==0.12.2
safetensors>=0.5,<0.6
soundfile>=0.12,<0.14
huggingface-hub>=0.31,<1
hydra-core==1.0.7
omegaconf==2.0.6
protobuf>=4.25
faster-whisper>=1.1,<2
silero-vad>=6.2,<7

# --- Phase 6 infra hardening ---
celery>=5.4,<6
redis>=5.0,<6
psycopg[binary]>=3.1,<4
psycopg2-binary>=2.9,<3

# --- Phase 8 public-chain anchoring ---
web3>=7,<8
