version: "3.9"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SATYAVOICE_DETECTOR_MODE=mock
      - SATYAVOICE_DATABASE_URL=sqlite:////app/data/satyavoice.db
      - SATYAVOICE_CORS_ORIGINS=*
    volumes:
      - satyavoice-data:/app/data

volumes:
  satyavoice-data:
