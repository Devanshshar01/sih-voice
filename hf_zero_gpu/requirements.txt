# hf_zero_gpu Space requirements — MMS-300M-AntiDeepfake only.
# Pinned per the official model card (verified 2026-09):
#   huggingface-hub==0.31.1, fairseq==0.12.2, safetensors==0.5.3,
#   soundfile==0.13.1 — these are the versions the published checkpoint
#   and its PyTorchModelHubMixin loading path were built against.
# NOTE: fairseq 0.12.2 is an older-style build (needs numpy<2 at runtime for
# its data classes in some environments) — this file is used ONLY inside the
# ZeroGPU Space, never installed into the Render backend venv.
gradio>=4.44,<5
spaces>=0.30
torch>=2.2
torchaudio>=2.2
fairseq==0.12.2
safetensors==0.5.3
soundfile==0.13.1
huggingface-hub==0.31.1
numpy>=1.26,<2
